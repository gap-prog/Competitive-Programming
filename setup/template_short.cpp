#include <bits/stdc++.h>
using namespace std;

#define spc " "
#define nl "\n"
#define ll long long
#define sz(x) ((int) x.size())
#define all(x) begin(x), end(x)

void solve() {
    // solve
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int t = 1;
    cin >> t;
    while (t--) solve();
    return 0;
}
