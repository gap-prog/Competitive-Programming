#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define nl "\n"
#define spc " "
#define sz(x) ((int) x.size())

int main() {
	ios::sync_with_stdio(false);
  cin.tie(NULL);
	int t;
	cin >> t;
	while (t--) {
		// Solve
	}
	return 0;
}
